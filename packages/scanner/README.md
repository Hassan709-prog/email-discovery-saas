# Scanner Core

Independent Python package for deterministic URL normalization, page ranking, crawling, extraction, filtering, and typed results.

This package must not import FastAPI, Celery, Redis clients, PostgreSQL models, billing, tenant logic, or UI contracts.
